// Example runtime configuration for the static frontend.
// Copy/rename this file to `env.js` during deployment and set the value below,
// or generate `env.js` in your build command using an environment variable.

window.ENV = window.ENV || {};
// Point to your deployed backend API base (include the /api suffix if applicable)
// Example:
// window.ENV.API_BASE_URL = "https://backend.finverge.madebyelio.com/api";

